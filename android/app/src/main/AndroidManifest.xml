<!--
    DEMONSTRATION PURPOSE ONLY
    This AndroidManifest.xml is intentionally configured insecurely for the ARGUS
    cybersecurity research platform. It demonstrates common Android banking malware
    characteristics detectable by static analysis.
    DO NOT USE IN PRODUCTION.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        DEMONSTRATION PURPOSE ONLY
        Intentionally insecure: Overly broad permissions declared for ARGUS detection.
        A legitimate banking app would request only the minimum required permissions.
        These permissions are declared but NOT exercised by the application logic.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        DEMONSTRATION PURPOSE ONLY
        Intentionally insecure: allowBackup=true allows ADB backup of application data.
        A real banking app should set allowBackup=false.

        DEMONSTRATION PURPOSE ONLY
        Intentionally insecure: debuggable=true exposes the app to debugging attacks.
        A production banking app must have android:debuggable="false" (or omit this attribute).

        DEMONSTRATION PURPOSE ONLY
        Intentionally insecure: usesCleartextTraffic=true allows HTTP plaintext connections.
        Banking apps must enforce HTTPS for all network traffic.
    -->
    <application
        android:label="Nova Bank"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:debuggable="true"
        android:usesCleartextTraffic="true">

        <!-- Main Flutter Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            DEMONSTRATION PURPOSE ONLY
            Intentionally insecure: InsecureWebViewActivity is exported with no restrictions,
            making it accessible to any app. This is a common attack surface in banking trojans.
            This activity is never launched by normal application flow.
        -->
        <activity
            android:name=".InsecureWebViewActivity"
            android:exported="true"
            android:label="Nova Bank WebView">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
        </activity>

        <!--
            DEMONSTRATION PURPOSE ONLY
            Intentionally insecure: MockOverlayService exported with no permissions guard.
            In real banking trojans, overlay services are used to display fake login screens
            on top of legitimate apps. This is a non-functional stub for ARGUS detection.
        -->
        <service
            android:name=".MockOverlayService"
            android:exported="true"
            android:permission="">
            <intent-filter>
                <action android:name="com.example.demobank1.OVERLAY_SERVICE" />
            </intent-filter>
        </service>

        <!--
            DEMONSTRATION PURPOSE ONLY
            Intentionally insecure: MockBootReceiver is exported and listens for BOOT_COMPLETED.
            Banking malware often uses boot receivers to auto-start persistence mechanisms.
            This receiver performs NO action — it is a stub for ARGUS detection only.
        -->
        <receiver
            android:name=".MockBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
            DEMONSTRATION PURPOSE ONLY
            Intentionally insecure: MockAccessibilityServiceStub is declared but android:enabled="false"
            so it is NEVER activated. Banking malware abuses AccessibilityService to read screen content.
            This stub exists solely for ARGUS static analysis detection.
        -->
        <service
            android:name=".MockAccessibilityServiceStub"
            android:exported="true"
            android:enabled="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="Nova Bank Accessibility (Demo Stub - Disabled)">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Flutter tool meta-data -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
